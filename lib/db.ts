/**
 * Database placeholder module.
 *
 * Prisma client initialization should be enabled after running:
 *   pnpm prisma generate
 * in an environment that can download Prisma engines.
 */
export const db = {
  isConfigured: false,
};
